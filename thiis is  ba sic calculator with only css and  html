<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>CSS Calculator (HTML + CSS only)</title>
    <style>
        :root{
            --bg:#0f1724;
            --panel:#0b1220;
            --accent:#06b6d4;
            --muted:#94a3b8;
            --key:#111827;
            --op:#1f2937;
            --glass: rgba(255,255,255,0.03);
            --shadow: 0 8px 30px rgba(2,6,23,0.6);
            --radius:14px;
        }

        *{box-sizing:border-box}
        html,body{height:100%}
        body{
            margin:0;
            font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
            background:
                radial-gradient(1200px 600px at 10% 10%, rgba(6,182,212,0.06), transparent 6%),
                radial-gradient(900px 400px at 90% 90%, rgba(99,102,241,0.03), transparent 8%),
                var(--bg);
            display:flex;
            align-items:center;
            justify-content:center;
            padding:32px;
            color:#e6eef6;
            -webkit-font-smoothing:antialiased;
            -moz-osx-font-smoothing:grayscale;
        }

        .calculator{
            width:min(380px,96%);
            background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent 30%);
            border-radius:var(--radius);
            padding:20px;
            box-shadow:var(--shadow);
            border:1px solid rgba(255,255,255,0.03);
            backdrop-filter: blur(6px);
        }

        /* top row: title + mode */
        .header{
            display:flex;
            align-items:center;
            justify-content:space-between;
            margin-bottom:12px;
        }
        .brand{
            display:flex;
            gap:10px;
            align-items:center;
        }
        .logo{
            width:40px;height:40px;border-radius:10px;
            background:linear-gradient(135deg,var(--accent),#667eea);
            display:flex;align-items:center;justify-content:center;font-weight:700;
            box-shadow: 0 6px 18px rgba(6,182,212,0.08), inset 0 -6px 18px rgba(255,255,255,0.02);
        }
        .title{font-size:14px;color:var(--muted)}
        .subtitle{font-size:12px;color:rgba(255,255,255,0.65)}

        /* screen */
        .screen{
            background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
            border-radius:12px;
            padding:14px;
            margin-bottom:18px;
            display:flex;
            align-items:center;
            justify-content:space-between;
            gap:12px;
            border:1px solid rgba(255,255,255,0.02);
        }
        .history{
            font-size:13px;
            color:var(--muted);
            min-height:18px;
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
            opacity:0.9;
        }
        .output{
            font-weight:600;
            font-size:28px;
            color:#fff;
            min-width:80px;
            text-align:right;
            line-height:1;
        }

        /* keys grid */
        .keys{
            display:grid;
            grid-template-columns: repeat(4, 1fr);
            gap:10px;
        }

        button.key{
            appearance:none;
            border:0;
            outline:0;
            padding:16px;
            border-radius:10px;
            background:linear-gradient(180deg,var(--glass), transparent);
            color:#e6eef6;
            font-size:18px;
            cursor:default; /* no JS: visually interactive but not clickable for logic */
            user-select:none;
            transition: transform .08s ease, box-shadow .08s ease, background .12s;
            box-shadow: 0 6px 14px rgba(2,6,23,0.6), inset 0 -6px 18px rgba(255,255,255,0.02);
        }

        button.key:active{
            transform: translateY(1px) scale(.997);
            box-shadow: 0 3px 8px rgba(2,6,23,0.6);
        }

        button.number{ background: linear-gradient(180deg,#0f1724,#071023); }
        button.operator{
            background: linear-gradient(180deg,#0b3b3f,#043238);
            color:#dff8fb;
            font-weight:600;
        }
        button.equal{
            grid-column: span 2;
            background: linear-gradient(180deg,var(--accent), #5eead4);
            color:#042027;
            font-weight:700;
            font-size:18px;
        }
        button.wide{ grid-column: span 2; }

        /* small helper labels on keys */
        .klabel{
            display:block;
            font-size:11px;
            color:var(--muted);
            opacity:0.9;
            margin-bottom:6px;
        }

        /* responsive */
        @media (max-width:420px){
            .output{font-size:24px}
            button.key{padding:14px;font-size:17px}
        }
    </style>
</head>
<body>
    <main class="calculator" role="application" aria-label="Calculator (HTML and CSS only)">
        <div class="header">
            <div class="brand">
                <div class="logo">C</div>
                <div>
                    <div class="title">CSS Calculator</div>
                    <div class="subtitle">HTML + CSS only — no JavaScript</div>
                </div>
            </div>
            <div style="font-size:12px;color:var(--muted);">Static UI</div>
        </div>

        <section class="screen" aria-hidden="true">
            <div class="history">0</div>
            <div class="output" aria-live="polite">0</div>
        </section>

        <section class="keys" role="grid">
            <!-- row 1 -->
            <button   onclick="clearDisplay()" class="key" type="button" aria-label="clear">
                <span class="klabel">C</span>AC
            </button>
            <button  onclick="del()"  class="key" type="button" aria-label="plus minus">
                <span class="klabel">D</span>±
            </button>
            <button onclick="num('%')" class="key operator" type="button" aria-label="percent">
                <span class="klabel">%</span>%
            </button>
            <button  onclick="num('/')" class="key operator" type="button" aria-label="divide">÷</button>

            <!-- row 2 -->
            <button onclick="num(7)" class="key number" type="button" aria-label="seven">7</button>
            <button onclick="num(8)" class="key number" type="button" aria-label="eight">8</button>
            <button onclick="num(9)" class="key number" type="button" aria-label="nine">9</button>
            <button onclick="num('*')" class="key operator" type="button" aria-label="multiply">×</button>

            <!-- row 3 -->
            <button onclick="num(4)" class="key number" type="button" aria-label="four">4</button>
            <button onclick="num(5)" class="key number" type="button" aria-label="five">5</button>
            <button onclick="num(6)" class="key number" type="button" aria-label="six">6</button>
            <button onclick="num('-')" class="key operator" type="button" aria-label="minus">−</button>

            <!-- row 4 -->
            <button onclick="num(1)" class="key number" type="button" aria-label="one">1</button>
            <button  onclick="num(2)"class="key number" type="button" aria-label="two">2</button>
            <button onclick="num(3)" class="key number" type="button" aria-label="three">3</button>
            <button onclick="num('+')" class="key operator" type="button" aria-label="plus">+</button>

            <!-- row 5 -->
            <button onclick="num(0)" class="key wide number" type="button" aria-label="zero">0</button>
            <button onclick="num('.')" class="key number" type="button" aria-label="decimal">.</button>
            <button onclick="solve('=')" class="key equal" type="button" aria-label="equals">=</button>
        </section>
    </main>
  <script>

 function num(val){
        document.getElementById("display").value +=val;

    }
    
  </script>

   

</body>

</html>
